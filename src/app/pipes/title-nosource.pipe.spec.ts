import { TitleNosourcePipe } from './title-nosource.pipe';

describe('TitleNosourcePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleNosourcePipe();
    expect(pipe).toBeTruthy();
  });
});
